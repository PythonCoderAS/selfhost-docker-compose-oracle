server {
    host warrior-oracle-host-(2|3).oracle.aoyans.dev;
    include /etc/nginx/oracle-host-$1.oracle.aoyans.dev-ssl;

    location / {
        proxy_pass http://warrior:8001/;
        proxy_set_header   X-Forwarded-Host   $http_host;
        proxy_set_header   X-Forwarded-For    $remote_addr;
    }
}
